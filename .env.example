# Compose reads production values from protected files under /etc/brai-new.
# This file documents names only and must never contain real credentials.
BRAI_WEB_BIND=127.0.0.1:3200
BRAI_GATEWAY_BIND=127.0.0.1:3201

